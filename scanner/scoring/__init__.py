"""Scoring package. Default mode is 'agent' (live, in-session reasoning).

The optional 'llm_api' mode (scoring/llm_scorer.py) is a clearly-marked stub and
is OFF by default — no API key is required for the tool to function.
"""
